# interview-code-review

Code-review exercises used in technical interviews. One self-contained project
per exercise:

```
exercises/<position>-<level>/
```

Each is an ordinary Flutter app - `flutter pub get`, `flutter analyze`,
`flutter test`. See the project's own README for what it does.
